[alias]
  a = add
  aa = add --all
  co = checkout
  com = checkout master
  cmm = commit
  d = diff --color | diff-so-fancy  | less --tabs=4 -RFX
  g = grep
  l = log --pretty=format:'%Cred%h%Creset @ %Cgreen%cr%Creset: %f %C(bold)%d%Creset %C(bold blue)<%an>%Creset' --abbrev-commit
  pom = pull origin master
  sh = show --name-only
  st = status -sb
  rug = stash
